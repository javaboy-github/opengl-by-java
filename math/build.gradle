plugins {
}

repositories {
	mavenCentral()
}

dependencies {
}

sourceSets {
	main {
		scala {
			srcDirs 'src'
		}
	}
}

tasks.test {
	useJUnitPlatform()
	testLogging {
		events "passed", "skipped", "failed"
	}
}

build.dependsOn test
